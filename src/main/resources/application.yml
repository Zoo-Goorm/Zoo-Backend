spring:
  application:
    name: InsightNote
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

kakao:
  api:
    cid: TC0ONETIME
    admin-key: ${KAKAO_API_KEY}